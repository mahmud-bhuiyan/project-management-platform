# Neon pooled URL for the app runtime (Prisma adapter)
DATABASE_URL=

# Optional: Neon direct (non-pooler) URL for migrations if pooled URL fails
# DIRECT_URL=

FRONTEND_URL=http://localhost:4200
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
PORT=3000
